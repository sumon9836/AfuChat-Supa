I need a complete startup crash audit for the AfuChat Android app.

The app installs and launches, shows the splash screen, then immediately crashes with an Android system crash notification. The build and dependency installation complete successfully, so I suspect a runtime startup issue.

Perform a thorough investigation of every feature, service, module, and initialization task that runs before the first screen is fully rendered.

Your tasks:

1. Identify every feature that executes during app startup, including but not limited to:
   
   - Authentication providers.
   - Expo Router layouts.
   - Splash screen handling.
   - Font loading.
   - Theme providers.
   - React Context providers.
   - Navigation containers.
   - Firebase or Supabase initialization.
   - Database connections.
   - Push notifications.
   - Deep linking.
   - Analytics and crash reporting.
   - Background tasks.
   - Background fetch.
   - Audio services and react-native-track-player.
   - react-native-worklets and Reanimated.
   - Gesture Handler.
   - SecureStore and AsyncStorage.
   - Network requests.
   - Environment variable loading.
   - File system initialization.
   - Any custom startup hooks.

2. For each startup feature:
   
   - Explain what it does.
   - Determine whether it can crash the app during startup.
   - Identify missing dependencies or configuration issues.
   - Check for unhandled exceptions.
   - Check for null or undefined values.
   - Check for Android-specific compatibility issues.
   - Verify compatibility with the current Expo SDK and React Native version.

3. Review:
   
   - app/_layout.tsx
   - app/index.tsx
   - Root providers.
   - package.json.
   - app.json or app.config.
   - eas.json.
   - babel.config.js.
   - metro.config.js.
   - AndroidManifest configuration.
   - Native module registrations.

4. Produce a startup dependency chain showing the exact order of execution from:
   App launch → Splash screen → Providers → Services → Navigation → First screen.

5. Flag every suspicious startup task with a risk level:
   
   - Critical
   - High
   - Medium
   - Low

6. Recommend which startup tasks should be temporarily disabled for isolation testing and in what order.

7. Identify any startup code that should be lazy-loaded or deferred until after the first screen renders.

8. Search for any startup task that could cause an immediate splash-screen crash without producing an obvious JavaScript error.

9. Do not simply report warnings. Provide concrete evidence for every suspicious component and explain why it could terminate the app.

10. Deliver a final report containing:
    
    - Suspected crash causes ranked by probability.
    - Exact files and code locations involved.
    - Required fixes.
    - Safe temporary workarounds.
    - A minimal startup configuration that should allow the app to launch successfully for testing.

The objective is to identify every startup component capable of causing an Android splash-screen crash and isolate the root cause systematically rather than making random changes.