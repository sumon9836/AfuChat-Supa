We need to prepare AfuChat for Google Play production release by temporarily removing or disabling high-risk Android permissions and sensitive features that are triggering Play Console policy reviews.

Please make the following changes:

1. Remove VoIP calling features for now

* Disable audio/video calling functionality completely
* Remove any background call services
* Remove all related permissions and services including:

  * FOREGROUND_SERVICE_PHONE_CALL
  * FOREGROUND_SERVICE_MEDIA_PLAYBACK
* Remove any active call notifications or background media playback tied to calls

2. Remove precise location sharing

* Disable live/exact location sharing in chats
* Remove location-related permissions if no longer needed:

  * ACCESS_FINE_LOCATION
  * ACCESS_COARSE_LOCATION
* Ensure no user GPS/location is shared with other users

3. Remove APK installation capability

* Remove REQUEST_INSTALL_PACKAGES permission
* Remove any APK installer/update installer logic
* Remove any libraries/plugins that request install packages permission
* Ensure app no longer attempts to install external APK files

4. Keep media upload functionality

* Keep image and video upload/sharing features
* Keep:

  * READ_MEDIA_IMAGES
  * READ_MEDIA_VIDEO
* Ensure media access is only user-initiated through gallery/file picker
* Prefer Android Photo Picker implementation where possible

5. Clean Android permissions and manifests

* Review AndroidManifest.xml and Expo/React Native config
* Remove unused sensitive permissions
* Ensure only required permissions remain in final AAB

6. Build new production-safe release

* Increment version code
* Generate new signed AAB
* Optimize release build for performance and stability
* Verify no removed permissions still exist in final manifest

Goal:
Prepare a clean Google Play–safe AfuChat release with messaging, social feed, AI, media sharing, and Acoin features while minimizing Play Store policy risks and approval delays.
